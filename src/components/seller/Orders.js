import React from 'react';
import OrderHistory from './OrderHistory';

const Orders = () => {
    return (
        <div>
            <OrderHistory/>
            <OrderHistory/>
        </div>
    );
};

export default Orders;